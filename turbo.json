{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**", ".next/**"] },
    "test": { "dependsOn": ["^build"], "passThroughEnv": ["DATABASE_URL", "REDIS_URL"] },
    "typecheck": {},
    "lint": {}
  }
}
